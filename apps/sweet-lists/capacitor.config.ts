import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sweet-lists',
  webDir: '../../dist/apps/sweet-lists',
  bundledWebRuntime: false,
};

export default config;
