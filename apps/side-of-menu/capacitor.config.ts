import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'side-of-menu',
  webDir: '../../dist/apps/side-of-menu',
  bundledWebRuntime: false,
};

export default config;
